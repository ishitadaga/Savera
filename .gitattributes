# Large CSV files use LFS (note: these are not used anymore, only summary file is needed)
backend/data/*_Interconnection_*.gz filter=lfs diff=lfs merge=lfs -text
# installer_summary.json.gz is small (~500KB) and should be committed directly
